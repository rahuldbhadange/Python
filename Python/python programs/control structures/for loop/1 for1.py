'''for loop: for loop executes the set of stmts with respect to
every element of the collection object'''

x=[10,20,30,40,50]
for p in x:      # p indicates each element of x,takes one element after another
    print(p,end=',')     # prints each element
