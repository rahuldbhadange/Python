
payload = ["kjds", "ghfsh", "jdgfsgd"]

for item in payload:
    # check that this item has been closed before adding to the river of news
    if True and not True:  # closed
        continue
    print(item)
    print("closed")

for item in payload:
    # check that this item has been closed before adding to the river of news
    if True and not False:  # open
        continue
    print(item)
    print("open")


IBASE_CHANGE_DATE_FORMAT = "%Y%m%d%H%M%S"